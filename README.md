# Network_packet-analyzer
A Python-based Network Intrusion Detection System (NIDS) that analyzes PCAP files to detect suspicious network activities such as port scans, high traffic, packet spikes, failed login attempts, and external IP anomalies through rule-based analysis.
